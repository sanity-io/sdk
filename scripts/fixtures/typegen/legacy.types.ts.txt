// Generated by @sanity/codegen@3.88.1-typegen-experimental.0.
// Two schemas declare post.title as string (primary) and number (secondary).
// Queries before migration:
//   import {defineQuery, defineProjection} from 'groq'
//   export const posts = defineQuery('*[_type == "post"]{title}')
//   export const preview = defineProjection('{title}')
// Kept as text so module augmentations only enter the packed consumer's program.

import type {ProjectionBase, PickSchema, SchemaOrigin} from 'groq'

export declare const internalGroqTypeReferenceTo: unique symbol

export type Post =
  | SchemaOrigin<
      {
        _id: string
        _type: 'post'
        _createdAt: string
        _updatedAt: string
        _rev: string
        title: string
      },
      'example.primary'
    >
  | SchemaOrigin<
      {
        _id: string
        _type: 'post'
        _createdAt: string
        _updatedAt: string
        _rev: string
        title: number
      },
      'example.secondary'
    >

export type AllSanitySchemaTypes = Post

// Source: schema.primary.json
// Schema ID: example.primary
export type ExamplePrimarySchema = PickSchema<Post, 'example.primary'>

// Source: schema.secondary.json
// Schema ID: example.secondary
export type ExampleSecondarySchema = PickSchema<Post, 'example.secondary'>

declare module 'groq' {
  interface SanitySchemas {
    'example.primary': ExamplePrimarySchema
    'example.secondary': ExampleSecondarySchema
  }
}

// Source: queries.ts
// Variable: posts
// Query: *[_type == "post"]{title}
export type PostsResult =
  | SchemaOrigin<
      Array<{
        title: string
      }>,
      'example.primary'
    >
  | SchemaOrigin<
      Array<{
        title: number
      }>,
      'example.secondary'
    >

// Source: queries.ts
// Variable: preview
// Projection: {title}
export type PreviewProjectionResult =
  | SchemaOrigin<
      ProjectionBase<
        {
          title: string
        },
        'post'
      >,
      'example.primary'
    >
  | SchemaOrigin<
      ProjectionBase<
        {
          title: number
        },
        'post'
      >,
      'example.secondary'
    >

declare module 'groq' {
  interface SanityQueries {
    '*[_type == "post"]{title}': PostsResult
  }
  interface SanityProjections {
    '{title}': PreviewProjectionResult
  }
}
