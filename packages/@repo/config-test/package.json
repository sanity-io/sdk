{
  "name": "@repo/config-test",
  "version": "0.0.1",
  "private": true,
  "description": "Test (as in unit test) config shared across packages",
  "type": "module",
  "exports": {
    "./vitest": "./vitest.mjs"
  },
  "scripts": {
    "lint": "eslint ."
  },
  "devDependencies": {
    "@repo/config-eslint": "workspace:*",
    "@repo/dev-aliases": "workspace:*",
    "vitest": "^3.1.1"
  }
}
