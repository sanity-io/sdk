import {deployCommand} from '../../_internal/cli/commands/deployCommand'
import {versionCommand} from '../../_internal/cli/commands/versionCommand'

/**
 * @internal
 */
export const SDKCommands = [versionCommand, deployCommand]
