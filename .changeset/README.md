# Changesets

This folder holds [changesets](https://github.com/changesets/changesets). Each `.md` file
describes a user-facing change and the version bump it should trigger.

Run `pnpm changeset` to add one. See [`CONTRIBUTING.md`](../CONTRIBUTING.md) for the bump
convention and the release flow.
