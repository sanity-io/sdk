---
'@sanity/sdk': minor
'@sanity/sdk-react': minor
---

Add the `useOAuthTokens` hook exposing stored OAuth token state with `refresh` and `revoke` ([#1213](https://github.com/sanity-io/sdk/pull/1213))
