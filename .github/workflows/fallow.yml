name: Fallow

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fallow:
    runs-on: ubuntu-latest
    env:
      TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
      TURBO_TEAM: ${{ vars.TURBO_TEAM }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0

      - name: Setup Environment
        uses: ./.github/actions/setup
        with:
          node-version: 20

      - name: Summary
        run: pnpm fallow --summary --quiet || true

      - name: Audit changed files
        run: |
          BASE_REF="${{ github.event.pull_request.base.ref || 'main' }}"
          git fetch origin "$BASE_REF" --depth=1 || true
          AUDIT_OUTPUT=$(pnpm --silent fallow audit --base "origin/$BASE_REF" --format markdown --quiet) || AUDIT_EXIT=$?
          {
            echo "## Fallow audit"
            echo ""
            echo "$AUDIT_OUTPUT"
          } >> "$GITHUB_STEP_SUMMARY"
          exit "${AUDIT_EXIT:-0}"

      - name: Hotspots
        if: always()
        run: |
          {
            echo "## Hotspots"
            echo ""
            echo '```'
            pnpm fallow health --hotspots --ownership --production --quiet || true
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
