{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "local>sanity-io/renovate-config",
    "helpers:pinGitHubActionDigests",
    ":configMigration",
    "abandonments:recommended"
  ],
  "ignorePresets": ["github>sanity-io/renovate-config:group-non-major"],
  "postUpdateOptions": ["pnpmDedupe"],
  "schedule": ["before 5am"],
  "prConcurrentLimit": 20,
  "branchConcurrentLimit": 5,
  "prHourlyLimit": 5,
  "automerge": true,
  "platformAutomerge": true,
  "automergeType": "pr",
  "automergeStrategy": "squash",
  "dependencyDashboard": true,
  "dependencyDashboardApproval": false,
  "dependencyDashboardHeader": "## How to use this dashboard\n\nFull playbook: [RENOVATE_STRATEGY.md](../blob/main/.github/RENOVATE_STRATEGY.md)\n\n**Quick section reference**\n\n- **Pending Approval**: should stay empty; we no longer gate updates behind dashboard approval.\n- **Awaiting Schedule** / **Rate-Limited**: automatic, no action needed.\n- **Pending Status Checks**: investigate if an item remains after a later Renovate run.\n- **PR Closed (Blocked)**: previously closed. Only tick to retry.\n- **Detected Dependencies**: reference only.\n\nRelease-age policy follows the shared Sanity preset. The playbook documents the scoped timestamp exceptions for GitHub Action digests and lock file maintenance.\n\n<details>\n<summary><b>Common actions</b></summary>\n\n**Retry a closed PR**: find it under \"PR Closed (Blocked)\" and tick the box.\n\n**A breaking major landed**: majors automerge after CI and applicable release-age checks clear. If a major breaks our public API, change its commit type from `fix(deps)` to `feat(deps)!` (or add a `BREAKING CHANGE:` footer) when squashing so release-please produces the right version bump.\n\n**A PR won't automerge**: check CI, the release-age status, and approval. A GitHub Action digest should not remain pending after a later Renovate run; see the playbook if it does.\n\n**A lockfile looks broken**: run `pnpm install` locally, commit, and push to the Renovate branch. Renovate won't overwrite manual commits.\n\n**Rate-limited queue is huge**: drain it by merging open Renovate PRs. The throttles are `prHourlyLimit` (currently 5) and `prConcurrentLimit` (currently 20) in `.github/renovate.json`.\n\n**The pnpm version** comes from the root `package.json` `packageManager` field.\n\n</details>",
  "prCreation": "immediate",
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": ["security"],
    "minimumReleaseAge": null,
    "automerge": false
  },
  "packageRules": [
    {
      "description": "Allow action digest branches after the release-age lookup passes",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["digest", "pinDigest"],
      "minimumReleaseAgeBehaviour": "timestamp-optional"
    },
    {
      "description": "Let pnpm enforce release age during lock file maintenance",
      "matchUpdateTypes": ["lockFileMaintenance"],
      "minimumReleaseAge": null
    },
    {
      "description": "Group @sanity/* deps; production, triggers a patch release",
      "matchPackageNames": ["@sanity/*"],
      "groupName": "sanity",
      "rangeStrategy": "bump",
      "semanticCommitType": "fix",
      "semanticCommitScope": "deps"
    },
    {
      "description": "All other production deps; triggers a patch release",
      "matchDepTypes": ["dependencies"],
      "rangeStrategy": "bump",
      "semanticCommitType": "fix",
      "semanticCommitScope": "deps"
    },
    {
      "description": "devDependencies; no release",
      "matchDepTypes": ["devDependencies"],
      "groupName": "devDependencies (non-major)",
      "semanticCommitType": "chore",
      "semanticCommitScope": "dev-deps"
    },
    {
      "description": "Group eslint deps",
      "matchPackageNames": [
        "@eslint/*",
        "eslint",
        "eslint-plugin-*",
        "eslint-config-*",
        "typescript-eslint"
      ],
      "groupName": "eslint-tooling",
      "semanticCommitType": "chore",
      "semanticCommitScope": "tooling"
    },
    {
      "description": "Group vitest deps",
      "matchPackageNames": ["vitest", "@vitest/*"],
      "groupName": "vitest-tooling",
      "semanticCommitType": "chore",
      "semanticCommitScope": "tooling"
    },
    {
      "description": "Group commitlint deps",
      "matchPackageNames": ["@commitlint/*", "commitlint"],
      "groupName": "commitlint-tooling",
      "semanticCommitType": "chore",
      "semanticCommitScope": "tooling"
    },
    {
      "description": "Group react deps",
      "matchPackageNames": ["react", "react-dom", "@types/react", "@types/react-dom"],
      "groupName": "react",
      "semanticCommitType": "chore",
      "semanticCommitScope": "dev-deps"
    },
    {
      "description": "App deps; no release",
      "matchFileNames": ["apps/**/package.json"],
      "groupName": "app dependencies",
      "semanticCommitType": "chore",
      "semanticCommitScope": "apps"
    },
    {
      "description": "Pin groq and @sanity/codegen to the typegen-experimental prerelease; don't bump to stable",
      "matchDepNames": ["groq", "@sanity/codegen"],
      "enabled": false
    }
  ]
}
